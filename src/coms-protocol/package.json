{
	"name": "agentharness-coms-protocol",
	"version": "0.1.0",
	"private": true,
	"description": "Shared wire-protocol + registry + transport helpers for the agentharness-comms layer.",
	"type": "module",
	"main": "index.ts",
	"scripts": {
		"typecheck": "tsc --noEmit"
	},
	"devDependencies": {
		"@types/node": "^22.20.1",
		"typescript": "^5.6.3"
	}
}
