# Ignore node_modules to prevent mounting conflicts during build context transfer
node_modules
npm-debug.log
yarn-error.log

# Ignore git metadata to keep build context light (unless strictly needed for git-based installs inside build)
.git
.gitignore

# Ignore local environment files
.env
.env.local

# Ignore docker artifacts
docker-compose.yml
Dockerfile
Dockerfile.*
Makefile

# Ignore system files
.DS_Store
Thumbs.db
